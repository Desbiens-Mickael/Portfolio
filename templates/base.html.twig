<!DOCTYPE html>
<html lang="fr" class="h-100">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta name="description" content="Portfolio de Mickaël Desbiens développeur web junior formé à la
         Wild Code School, passionné par le web et la programmation informatique.">
        <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview: large">
        <title>{% block title %}Portfolio de Mickaël Desbiens | Développeur Web Junior | {% endblock %}</title>
        <link rel="shortcut icon"  type="image/png" href="{{ asset('build/images/logo-initial.png') }}">
        <link rel="canonical" href="https://mickaeldesbiens.com">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="h-100 d-flex flex-column clean-transition"
    >
        {% include 'include/_navbar.html.twig' %}
        <main id="swup">
            <div class="background-img"></div>
            {% for category, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-dismissible mt-5 w-75 mx-auto alert-{{ category }}" role="alert" id="flash">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endfor %}
            {% endfor %}
            {% block body %}{% endblock %}
        </main>
        {% include 'include/_footer.html.twig' %}
        <script src="https://www.google.com/recaptcha/api.js?onload=onloadCallback&render=explicit"
                async defer>
        </script>
    </body>
</html>
